import {LockFilterPipe} from './lock-filter.pipe';

describe('LockFilterPipe', () => {
    it('create an instance', () => {
        const pipe = new LockFilterPipe();
        expect(pipe).toBeTruthy();
    });
});
